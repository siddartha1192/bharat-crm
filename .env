# Backend API URL (use relative path so it works through nginx proxy)
VITE_API_URL=/api

# Database Configuration (REQUIRED for postgres container)
POSTGRES_DB=bharat_crm
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_secure_password_123

# JWT Secrets (REQUIRED for backend)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long-change-this
JWT_REFRESH_SECRET=your-super-secret-refresh-key-minimum-32-characters-change-this

# OpenAI API (REQUIRED for AI features)
OPENAI_API_KEY=sk-your-openai-api-key-here

# WhatsApp Business API (Optional - can be empty for now)
WHATSAPP_API_TOKEN=
WHATSAPP_PHONE_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=

# Google OAuth & Calendar (Update with your actual domain)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://climcrm.com:8080/api/auth/google/callback

# Email Configuration (Optional - can be empty for now)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=

# Frontend URL (Update with your actual domain)
FRONTEND_URL=http://climcrm.com:8080

# Domain Configuration (for production SSL - leave as is for local testing)
DOMAIN=localhost
SSL_EMAIL=admin@localhost
